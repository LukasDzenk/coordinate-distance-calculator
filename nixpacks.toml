[variables]
NIXPACKS_NODE_VERSION = "24"

[phases.setup]
node_version = "24"

[phases.install]
cmds = [
  "npm install -g corepack@0.34.7",
  "corepack enable",
  "pnpm i --frozen-lockfile --prod=false",
]
